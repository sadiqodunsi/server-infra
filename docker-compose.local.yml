# =============================================================================
# Local Entrypoint - HTTP Only (No SSL)
# =============================================================================
# docker compose -f docker-compose.local.yml up -d
# Includes staging (Postgres) plus docker-compose.local.overrides.yml for HTTP
# routers and loopback Postgres/Redis ports. Requires hosts file (see README).
# =============================================================================
include:
  - docker-compose.staging.yml
  - docker-compose.local.overrides.yml
